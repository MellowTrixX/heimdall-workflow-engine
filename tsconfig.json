{
  "compilerOptions": {
    "module": "commonjs",
    "target": "ES2020",
    "outDir": "out",
    "lib": ["ES2020"],
    "sourceMap": true,
    "rootDir": "src",
    "strict": true, /* enable all strict type-checking options */
    "types": ["node"],  // ← Fix: Node.js-Typen (path, https, fs etc.)
    "esModuleInterop": true  // ← Fix: Bessere Interop für require/import
  },
  "include": [
    "src/**/*"  // ← Neu: Compiliert nur Haupt-Code, skippt Tests (src/test bleibt unberührt)
  ],
  "exclude": [
    "node_modules",
    ".vscode-test",
    "src/test/**/*"  // ← Neu: Explizit Tests ausschließen (für Sicherheit)
  ]
}